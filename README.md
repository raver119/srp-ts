###SRP6 "no-crypto"

This repository contains SRP6 Client implementation, 100% compatible to famous Mozilla's [`node-srp`](https://github.com/mozilla/node-srp), but without dependencies to NodeJS Crypto.
Thus, it has way smaller footprint.

External dependencies are: `jsbn` and `hash.js`